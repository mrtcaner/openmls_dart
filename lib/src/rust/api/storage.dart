// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import '../frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';
import 'types.dart';

// These functions are ignored because they are not marked as `pub`: `batch_from_provider`, `batch_from_updates`, `provider_from_entries`, `validate_storage_entries`, `zeroize_entry_values`

/// Return the only storage format version accepted by this build.
int mlsStorageFormatVersion() =>
    RustLib.instance.api.crateApiStorageMlsStorageFormatVersion();

/// Create a key package without performing any durable write.
///
/// On success, the caller must apply the complete returned batch or discard it.
/// On failure, no batch is returned.
Future<CreateKeyPackageWithStorageResult> createKeyPackageWithStorage({
  required MlsCiphersuite ciphersuite,
  required List<int> signerBytes,
  required List<int> credentialIdentity,
  required List<int> signerPublicKey,
  Uint8List? credentialBytes,
  required List<MlsStorageEntry> storageEntries,
  required int storageFormatVersion,
}) => RustLib.instance.api.crateApiStorageCreateKeyPackageWithStorage(
  ciphersuite: ciphersuite,
  signerBytes: signerBytes,
  credentialIdentity: credentialIdentity,
  signerPublicKey: signerPublicKey,
  credentialBytes: credentialBytes,
  storageEntries: storageEntries,
  storageFormatVersion: storageFormatVersion,
);

/// Create an application message and return its sender-state changes.
Future<CreateMessageWithStorageResult> createMessageWithStorage({
  required List<int> groupId,
  required List<int> signerBytes,
  required List<int> message,
  required List<int> aad,
  required List<MlsStorageEntry> storageEntries,
  required int storageFormatVersion,
}) => RustLib.instance.api.crateApiStorageCreateMessageWithStorage(
  groupId: groupId,
  signerBytes: signerBytes,
  message: message,
  aad: aad,
  storageEntries: storageEntries,
  storageFormatVersion: storageFormatVersion,
);

/// Delete a group and represent the complete group removal in one batch.
Future<MlsStorageBatch> deleteGroupWithStorage({
  required List<int> groupId,
  required List<MlsStorageEntry> storageEntries,
  required int storageFormatVersion,
}) => RustLib.instance.api.crateApiStorageDeleteGroupWithStorage(
  groupId: groupId,
  storageEntries: storageEntries,
  storageFormatVersion: storageFormatVersion,
);

/// Key package bytes and the state changes that created them.
class CreateKeyPackageWithStorageResult {
  final Uint8List keyPackageBytes;
  final MlsStorageBatch storageBatch;

  const CreateKeyPackageWithStorageResult({
    required this.keyPackageBytes,
    required this.storageBatch,
  });

  @override
  int get hashCode => keyPackageBytes.hashCode ^ storageBatch.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CreateKeyPackageWithStorageResult &&
          runtimeType == other.runtimeType &&
          keyPackageBytes == other.keyPackageBytes &&
          storageBatch == other.storageBatch;
}

class CreateMessageWithStorageResult {
  final Uint8List ciphertext;
  final MlsStorageBatch storageBatch;

  const CreateMessageWithStorageResult({
    required this.ciphertext,
    required this.storageBatch,
  });

  @override
  int get hashCode => ciphertext.hashCode ^ storageBatch.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is CreateMessageWithStorageResult &&
          runtimeType == other.runtimeType &&
          ciphertext == other.ciphertext &&
          storageBatch == other.storageBatch;
}

/// All durable changes produced by one successful MLS operation.
class MlsStorageBatch {
  final List<MlsStorageEntry> upserts;
  final List<Uint8List> deletes;
  final List<Uint8List> deletedGroupIds;
  final int storageFormatVersion;

  const MlsStorageBatch({
    required this.upserts,
    required this.deletes,
    required this.deletedGroupIds,
    required this.storageFormatVersion,
  });

  @override
  int get hashCode =>
      upserts.hashCode ^
      deletes.hashCode ^
      deletedGroupIds.hashCode ^
      storageFormatVersion.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MlsStorageBatch &&
          runtimeType == other.runtimeType &&
          upserts == other.upserts &&
          deletes == other.deletes &&
          deletedGroupIds == other.deletedGroupIds &&
          storageFormatVersion == other.storageFormatVersion;
}

/// One opaque OpenMLS storage row owned by the caller.
class MlsStorageEntry {
  final Uint8List key;
  final Uint8List value;

  /// `None` means installation-global state; otherwise this is group state.
  final Uint8List? groupId;

  const MlsStorageEntry({required this.key, required this.value, this.groupId});

  @override
  int get hashCode => key.hashCode ^ value.hashCode ^ groupId.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is MlsStorageEntry &&
          runtimeType == other.runtimeType &&
          key == other.key &&
          value == other.value &&
          groupId == other.groupId;
}
